<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">My Blog</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">

        <% if can? :read, Category.new %>
        <%= raw(controller.controller_name == "categories" ? "<li class='active'>" : "<li>")%>
		<%= link_to "Categories", categories_path %> </li>
		<%end%>

		<% if can? :read, Article.new %>
		<%= raw(controller.controller_name == "articles" ? "<li class='active'>" : "<li>")%>
		<%= link_to "Articles", articles_path %> </li>
		<%end%>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Categories <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% Category.all.each do |category| %>
            	<li><%= link_to category.name, category_path(category.id) %></li>
            <%end%>
          </ul>
        </li>
      </ul>
      
      <% if can? :create, Role.new %>
      <ul class="nav navbar-nav navbar-right">  
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Settings <span class="caret"></span></a>
          <ul class="dropdown-menu">
          <% if can? :read, Role.new %>
		<%= raw(controller.controller_name == "roles" ? "<li class='active'>" : "<li>")%>
		<%= link_to "Roles", roles_path %></li>
		<%end%>

		<% if can? :read, User.new %>
		<%= raw(controller.controller_name == "users" ? "<li class='active'>" : "<li>")%>
		<%= link_to "Users", users_path %></li>
		<%end%>
    <%end%>

          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

